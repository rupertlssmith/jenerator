<?xml version="1.0" encoding="UTF-8"?>

<project>
  <extend>${basedir}/../mavenbuild/project.xml</extend>
  <name>struts-common</name>
  <id>struts-common</id>
  <currentVersion>0.3</currentVersion>
  <shortDescription>The part of my common library that is struts dependant.</shortDescription>
  <description>The part of my common library that is struts dependant. Split of from the rest of the common library as it needs to be deployed in .war files with the struts jars.</description>
  <repository>
    <connection>scm:${scm.setup}/projects/struts-common</connection>
  </repository>

  <versions>
    <version>
      <id>0.1</id>
      <name>0.1</name>
      <tag>RELEASE_0_1</tag>
    </version>
    <version>
      <id>0.2</id>
      <name>0.2</name>
      <tag>RELEASE_0_2</tag>
    </version>
  </versions>

  <dependencies>

    <!-- Other module dependencies. -->
    <dependency>
      <groupId>thebadgerset</groupId>
      <artifactId>common</artifactId>
      <version>0.3</version>
    </dependency>

  </dependencies>

</project>
